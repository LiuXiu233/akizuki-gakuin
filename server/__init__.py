"""秋月学院 Web 后端。

把 ``engine/`` 的规则引擎包装成 HTTP API，并提供：

* 多用户 / 多存档隔离（每个世界一个独立数据目录）
* LLM 双格式代理（OpenAI / Anthropic）
* 可配置多 Agent 流水线
* 文生图（OpenAI 兼容 + 自定义模板）

**引擎本身不做任何妥协**：所有规则、判定、关系、存档逻辑仍然只在 ``engine/`` 里，
本包只负责传输、隔离与编排。
"""

__version__ = "1.0.0"
