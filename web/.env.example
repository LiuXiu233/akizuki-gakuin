# 后端地址（部署到 Vercel 时填你服务器的公网地址）
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# 可选：给 Vercel 边缘 LLM 代理用的服务端 key。
# 填了它，玩家不填 key 也能玩（会走 /api/llm，费用算你的）。
LLM_API_KEY=
LLM_BASE_URL=
LLM_PROVIDER=openai
LLM_MODEL=
